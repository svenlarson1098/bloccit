<h1>New Sponsored Post</h1>

<div class="row">
  <div class="col-md-4">
    <p> Guidelines for sponsored posts:</p>
      <ul>
        <li>No pyramid or Ponzi schemes.</li>
        <li>No Cryptocurrency "Fast $$$" posts.</li>
        <li>"Sell me this pen..."</li>
      </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@topic, @sponsored_post] do |f| %>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: "Enter Post Title" %>
    </div>
    <div class="form-group">
      <%= f.label :body %>
      <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter New Post " %>
    </div>
    <div class="form-group">
      <%= f.label :price %>
      <%= f.number_field :price, class: 'form-control', placeholder: "$" %>
  </div>
    <div class="form-group">
      <%= f.submit "Save", class: 'btn btn-success' %>
     </div>
   <% end %>    
  </div>
</div>

